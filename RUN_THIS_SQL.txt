========================================
COPY AND RUN THIS SQL IN YOUR DATABASE
========================================

The database is currently in use (your application is running).
Please run this SQL directly in your application's database console or H2 Console.

--- STEP 1: Update SMS Configuration ---

DELETE FROM sms_config;

INSERT INTO sms_config (api_key, api_base_url, sender_name, enabled, last_updated) 
VALUES (
    'sk_6bb6f6f6-391a-4b34-8146-d38bacf73d4b',
    'https://unismsapi.com/api',
    'BDMS',
    TRUE,
    CURRENT_TIMESTAMP
);

--- STEP 2: Verify Configuration ---

SELECT 
    SUBSTRING(api_key, 1, 10) as key_start,
    api_base_url,
    sender_name,
    enabled,
    last_updated
FROM sms_config;

--- Expected Result ---
-- key_start: sk_6bb6f6f
-- api_base_url: https://unismsapi.com/api
-- sender_name: BDMS
-- enabled: TRUE

========================================
AFTER RUNNING THE SQL ABOVE:
========================================

Close your application and run this command to test SMS:

java -cp ".;target/classes;C:\Users\Razza\.m2\repository\com\h2database\h2\2.1.214\h2-2.1.214.jar" com.example.TestUniSMS

========================================
